<h1>Tasks to do</h1>
<ul class="list-group">
  <% @tasks.each do |task| %>
    <li class="list-group-item"><%= link_to task.title, task_path(task) %> <i class="fa-solid fa-trash"></i></a></li>
  <% end %>
</ul>

<%= link_to "Add a new task", tasks_new_path, class: "d-flex justify-content-center btn btn-primary btn-sm w-100" %>
